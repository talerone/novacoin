#!/bin/sh

set -e

umask 077

basedir=~/.taler
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/taler.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# taler does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/taler/taler-qt "$@"
